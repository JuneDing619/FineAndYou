# FineAndYou

GitHub Pages 多项目站点仓库。

## 当前项目

- `JFH/`：数字化经营分析会场景介绍页

## 目录约定

- 根目录 `index.html`：项目总入口
- 子目录 `<project>/index.html`：单个项目正式页面

## 发布

建议开启 GitHub Pages，使用 `GitHub Actions` 方式部署。

当前仓库已包含 `Pages` 自动部署工作流，后续网页更新后可直接通过推送触发重新发布。
